# Server configuration
server:
   port: 8081
   servlet:
      context-path: /order
      encoding:
         charset: UTF-8
         enabled: true
         force: true

# Application configuration
app:
   services:
      identity:
         url: ${IDENTITY_SERVICE_URL:http://localhost:8080/identity}
      product:
         url: ${PRODUCT_SERVICE_URL:http://localhost:8082/product}

# Spring configuration
spring:
   application:
      name: order-service
   datasource:
      url: ${DBMS_URL:jdbc:mysql://localhost:3306/order_service}
      driverClassName: "com.mysql.cj.jdbc.Driver"
      username: ${DBMS_USERNAME:root}
      password: ${DBMS_PASSWORD:root}
   jpa:
      show-sql: true
      open-in-view: false
      hibernate:
         ddl-auto: update
   kafka:
      bootstrap-servers: localhost:9094
      producer:
         key-serializer: org.apache.kafka.common.serialization.StringSerializer
         value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
   servlet:
      multipart:
         max-file-size: 10MB
         max-request-size: 10MB

stripe:
   secret-key: ${STRIPE_SECRET_KEY:sk_test_51O41qGBy1BulLKF8u3JAYr4tDLkAXiR3UpjDbyJn3QR5Jfr84h62AVXmU9RPKgwjtIIUSKPKIlvsOZJeTUHfdzZu0013X6szi0}
