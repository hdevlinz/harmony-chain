# Server configuration
server:
   port: 8886
   servlet:
      context-path: /notification
      encoding:
         charset: UTF-8
         enabled: true
         force: true

# Spring configuration
spring:
   application:
      name: notification-service
   data:
      mongodb:
         uri: ${MONGODB_URI:mongodb://root:root@localhost:27017/notification_service?authSource=admin}
   kafka:
      bootstrap-servers: PLAINTEXT://kafka:9094
      consumer:
         auto-offset-reset: earliest
         group-id: group-notification
         key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
         value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
         properties:
            spring.json.trusted.packages: "*"
            spring.json.value.default.type: "com.tth.event.dto.NotificationEvent"
      listener:
         missing-topics-fatal: false

# Application configuration
app:
   services:
      email:
         url: ${EMAIL_SERVICE_URL:https://api.brevo.com}
         mail-api-key: ${MAIL_API_KEY}